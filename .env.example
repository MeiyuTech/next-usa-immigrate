# Database connection string
# For production (Vercel), use Supabase Connection Pooler:
# DATABASE_URI=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# For local development:
DATABASE_URI=postgres://postgres:<password>@127.0.0.1:5432/next-usa-immigrate
# Why Connection Pooler? 
# 1. Vercel uses IPv6 by default
# 2. Direct connection has IPv6 compatibility issues
# 3. Connection Pooler ensures IPv4 compatibility and better stability

# S3 Configuration
S3_BUCKET=supabase-s3-bucket
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_REGION=
S3_ENDPOINT=

# Alternative local connection string
#DATABASE_URI=postgresql://127.0.0.1:5432/payload-template-website

# JWT token encryption key - Change this in production!
PAYLOAD_SECRET=aa1a08dbc01555ee0bdb9892

# Server URL Configuration
# Development: http://localhost:3000
# Production: Your actual domain (set automatically by Vercel)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Email Configuration - Choose one option:

# Option 1: SMTP Configuration (e.g., Office 365)
SMTP_HOST=smtp-legacy.office365.com
SMTP_DEFAULT_FROM_NAME=Your Name
SMTP_USER=yourname@yourdomain.com
SMTP_PASS=yourpassword
SMTP_PORT=587
SMTP_SECURE=false

# Option 2: Resend API (Recommended for production)
RESEND_DEFAULT_FROM_ADDRESS=yourname@yourdomain.com
RESEND_DEFAULT_FROM_NAME=Your Name
RESEND_API_KEY=re_1234567890
